; generated by drom from package skeleton 'library'

(library
  (name autofonce_api)
  (public_name autofonce_api)
  (wrapped true)
  (libraries autofonce_lib )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_api))


